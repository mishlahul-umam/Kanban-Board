# Copy to .env next to docker-compose.yml (optional). docker compose loads it automatically.
# POSTGRES_PASSWORD=postgres
# JWT_SECRET=use-a-long-random-string-in-production
# CORS_ORIGIN=http://localhost
# FRONTEND_PORT=80
